function greeter(person) {
    return "Hello, " + person;
}
var user = "Jane User", age = 42;
export default function () { return document.body.innerHTML = greeter(user); };
//# sourceMappingURL=greeter.js.map